# Kitty session layout for Experiment 004 A/B test
# Launch with: kitty --start-as fullscreen --session ~/Desktop/development/engram/docs/experiments/004-kitty-session.conf

layout splits

# Left pane: Session A — Control (no recall)
title Session A: Control (no engram)
launch bash

# Right pane: Session B — Treatment (with recall)
title Session B: With Recall (engram)
launch --location=vsplit bash
